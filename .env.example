# Copy to .env for local development (never commit .env)

SECRET_KEY=change-me-local-dev
FLASK_DEBUG=1
PORT=5000

MODEL_PATH=models/deploy/model.pt
# MODEL_URL=https://your-host.com/model.pt

DEVICE=cpu
MAX_UPLOAD_MB=50
