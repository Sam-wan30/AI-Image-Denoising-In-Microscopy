# Production dependencies for the Streamlit deployment.
# Use the PyTorch CPU wheel index for consistent cloud installs.

--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0

streamlit>=1.28.0,<2.0.0
numpy>=1.24.0,<2.0.0
opencv-python-headless>=4.8.1,<5.0.0
pillow>=10.0.0,<12.0.0
